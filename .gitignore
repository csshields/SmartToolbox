node_modules/

data/
logs/

.env
.env.*
!.env.example

dist/
build/
build-esp32s3/
coverage/

# Vendor datasheets are indexed in docs/SOURCES.md and re-downloadable from
# the vendor URLs recorded there; the PDFs themselves stay out of the repo.
docs/*.pdf

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.DS_Store
Thumbs.db

# Deploy-time secrets, if any are ever added. sync.ps1 itself is tracked.
.deploy.env

# Real Wi-Fi and device credentials. The .example.h template is tracked instead.
firmware/arduino_secrets.h
